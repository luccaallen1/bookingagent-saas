services:
  - type: web
    name: booking-agent-saas
    runtime: node
    buildCommand: cd booking-agent-ui && npm install && npm run build
    startCommand: cd booking-agent-ui && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    autoDeploy: true